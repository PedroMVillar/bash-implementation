ls cd pwd mkdir touch cp mv rm rmdir cat less head tail grep find chmod chown chgrp tar zip unzip ssh scp wget curl ping ifconfig ps top kill du df uname man alias ln basename dirname stat file df du jobs bg fg nice renice nohup wait xargs uptime dmesg vmstat free who whoami w hostname netstat traceroute nslookup dig route ip gzip gunzip bzip2 bunzip2 xz nano vim emacs sed awk passwd useradd usermod groupadd groups tail journalctl syslog echo history date cal bc expr tee yes apt-get apt-cache dpkg yum dnf rpm gpg openssl
